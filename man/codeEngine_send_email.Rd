% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codeEngine_send_email.R
\name{codeEngine_send_email}
\alias{codeEngine_send_email}
\title{Send an email via Domo CodeEngine}
\usage{
codeEngine_send_email(
  user_id,
  subject,
  html_body,
  domo_instance,
  package_id,
  package_version,
  function_name,
  token,
  group_id = NULL,
  csv_file = NULL,
  csv_file_name = "attachment.csv",
  auth_mode = c("auto", "bearer", "developer")
)
}
\arguments{
\item{user_id}{Numeric Domo user ID of the recipient (e.g. \code{"123456789"}).}

\item{subject}{Email subject line.}

\item{html_body}{Full HTML string for the email body.}

\item{group_id}{Optional Domo group ID to include as a recipient.}

\item{csv_file}{Optional data frame, CSV text, or CSV file path. The function writes data frames to a temporary CSV file and uploads it as a FILE input.}

\item{csv_file_name}{Optional filename to use for the temporary uploaded CSV file.}

\item{domo_instance}{Your Domo instance name (e.g. \code{"your-instance"} for
\code{your-instance.domo.com}).}

\item{package_id}{CodeEngine package ID (UUID string).}

\item{package_version}{Package version string (e.g. \code{"1.0.0"}).}

\item{function_name}{Name of the CodeEngine function (e.g. \code{"sendCustomEmail"}).}

\item{token}{Domo bearer or developer token.}

\item{auth_mode}{One of \code{"auto"} (default), \code{"bearer"}, or
\code{"developer"}. \code{"auto"} tries bearer first, falls back to
developer on a 401.}
}
\value{
A named list with \code{sent} (logical), \code{user_id},
\code{http_status_code}, \code{auth_mode}, and \code{error_message}.
}
\description{
Triggers a Domo CodeEngine function to send a single HTML email to a Domo
user. The email is sent from Domo's notification email context, not directly
from your R session. Returns a named list — never throws — so it is safe to
use in a loop.
}
\examples{
\dontrun{
result <- codeEngine_send_email(
  user_id         = "123456789",
  subject         = "Subject of the Email",
  html_body       = "<p>Hello, your file is ready.</p>",
  domo_instance   = "your-instance",
  package_id      = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx",
  package_version = "1.0.0",
  function_name   = "sendCustomEmail",
  token           = domo_access_token,
  group_id        = "987654321",
  csv_file        = mtcars,
  csv_file_name   = "your_file.csv"
)
if (!result$sent) message("Failed: ", result$error_message)
}
}
